import React from 'react';
import {
  StyledContainer,
} from './styled';

export interface ISpacerProps {
}

const Spacer: React.VFC<ISpacerProps> = () => {
  return <StyledContainer />
};

export default Spacer;
